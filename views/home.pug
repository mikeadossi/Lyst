extends layout

block content
  .todrop_header
    .todrop_header_left
      a.home(href='/') Lyst
    .todrop_header_right
      a.todrop_login(href='/login') log in
      a.todrop_sign_up(href='/signup') sign up
  
  #landing_container
    .landing_header Welcome
